import type { Express } from 'express';
import type { Server } from 'ws';
export interface ServerConfig {
    port: number;
    cors?: boolean;
    app?: Express;
    webhookUrl?: string;
    socketServer?: Server;
}
