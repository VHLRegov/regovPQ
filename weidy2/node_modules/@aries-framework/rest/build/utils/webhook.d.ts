/// <reference types="node" />
import type { IncomingHttpHeaders } from 'http';
export declare const sleep: (ms: number) => Promise<unknown>;
export interface WebhookData {
    receivedAt: string;
    headers: IncomingHttpHeaders;
    body: {
        id: string;
        state: string;
        [key: string]: unknown;
    };
    topic: string;
}
export declare const webhookListener: (port: number, webhooksReceived: WebhookData[]) => Promise<import("http").Server>;
