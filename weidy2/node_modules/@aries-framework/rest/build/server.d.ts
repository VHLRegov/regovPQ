import 'reflect-metadata';
import type { ServerConfig } from './utils/ServerConfig';
import { Agent } from '@aries-framework/core';
export declare const setupServer: (agent: Agent, config: ServerConfig) => Promise<import("express-serve-static-core").Express>;
