import { Agent } from '@aries-framework/core';
export declare const genesisPath: string;
export declare const setupAgent: ({ name, publicDidSeed, endpoints, port, }: {
    name: string;
    publicDidSeed: string;
    endpoints: string[];
    port: number;
}) => Promise<Agent>;
