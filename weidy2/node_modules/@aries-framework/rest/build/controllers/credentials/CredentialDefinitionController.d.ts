import type { SchemaId } from '../examples';
import { Agent } from '@aries-framework/core';
import { Controller, TsoaResponse } from 'tsoa';
import { CredentialDefinitionId } from '../examples';
export declare class CredentialDefinitionController extends Controller {
    private agent;
    constructor(agent: Agent);
    /**
     * Retrieve credential definition by credential definition id
     *
     * @param credentialDefinitionId
     * @returns CredDef
     */
    getCredentialDefinitionById(credentialDefinitionId: CredentialDefinitionId, badRequestError: TsoaResponse<400, {
        reason: string;
    }>, notFoundError: TsoaResponse<404, {
        reason: string;
    }>, internalServerError: TsoaResponse<500, {
        message: string;
    }>): Promise<any>;
    /**
     * Creates a new credential definition.
     *
     * @param credentialDefinitionRequest
     * @returns CredDef
     */
    createCredentialDefinition(credentialDefinitionRequest: {
        schemaId: SchemaId;
        supportRevocation: boolean;
        tag: string;
    }, notFoundError: TsoaResponse<404, {
        reason: string;
    }>, internalServerError: TsoaResponse<500, {
        message: string;
    }>): Promise<any>;
}
