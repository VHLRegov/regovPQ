import type { BasicMessageRecord } from '@aries-framework/core';
import { Agent } from '@aries-framework/core';
import { Controller, TsoaResponse } from 'tsoa';
import { RecordId } from '../examples';
export declare class BasicMessageController extends Controller {
    private agent;
    constructor(agent: Agent);
    /**
     * Retrieve basic messages by connection id
     *
     * @param connectionId Connection identifier
     * @returns BasicMessageRecord[]
     */
    getBasicMessages(connectionId: RecordId): Promise<BasicMessageRecord[]>;
    /**
     * Send a basic message to a connection
     *
     * @param connectionId Connection identifier
     * @param content The content of the message
     */
    sendMessage(connectionId: RecordId, request: Record<'content', string>, notFoundError: TsoaResponse<404, {
        reason: string;
    }>, internalServerError: TsoaResponse<500, {
        message: string;
    }>): Promise<any>;
}
